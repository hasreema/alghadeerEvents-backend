# Backend configuration
DEBUG=true
HOST=0.0.0.0
PORT=8000

# PostgreSQL connection (used by the backend and Alembic)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/alghadeer

# Frontend/CORS
FRONTEND_URL=http://localhost:5173
# Comma-separated list, overrides FRONTEND_URL if set
CORS_ORIGINS=